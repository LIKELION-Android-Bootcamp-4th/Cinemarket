import 'package:flutter/material.dart';

class FixReviewComponent extends StatelessWidget {
  const FixReviewComponent({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
